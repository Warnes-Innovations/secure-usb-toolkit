name: Build & Validate

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: pip install pyinstaller

      - name: Validate shell scripts (syntax check only — no execution)
        run: |
          for f in build/*.sh; do
            echo "Checking $f ..."
            bash -n "$f"
          done

      - name: Validate Python modules import cleanly
        run: |
          python3 -c "from build import safety; print('safety.py OK')"
          python3 -c "import ast, sys; ast.parse(open('tui.py').read()); print('tui.py OK')"
          python3 -c "import ast, sys; ast.parse(open('cli.py').read()); print('cli.py OK')"

      - name: Build Linux launcher with PyInstaller
        run: |
          pyinstaller --onedir --name SecureUSB --distpath dist tui.py

      - name: Generate checksums
        run: |
          cd dist
          find . -type f | sort | xargs sha256sum > checksums.txt
          cat checksums.txt

      - name: Upload launcher artifact
        uses: actions/upload-artifact@v4
        with:
          name: secure-usb-launcher-linux
          path: dist/
